<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NotepadLocker" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/welcome</item>
        <item name="colorPrimaryVariant">@color/welcome</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--Login-->
    <style name="LoginColor" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/login</item>
        <item name="android:statusBarColor">@color/login</item>
        <item name="colorSecondary">@color/login</item>
    </style>

    <!--Register-->
    <style name="RegisterColor" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/register</item>
        <item name="android:statusBarColor">@color/register</item>
        <item name="colorSecondary">@color/register</item>
    </style>

    <!--Welcome Page-->
    <style name="WelcomePage" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/welcome</item>
        <item name="android:statusBarColor">@color/welcome</item>
        <item name="colorSecondary">@color/welcome</item>
        <item name="android:itemTextAppearance">@color/welcome</item>
    </style>

    <style name="FullScreenDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:layout_marginTop">100dp</item>
        <item name="android:layout_marginBottom">100dp</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>

    <style name="Theme.NotepadLocker.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.NotepadLocker.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.NotepadLocker.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>